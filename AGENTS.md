# MOSS AI 开发入口

本文件是项目根目录唯一的 AI 协作入口。产品、架构、数值和内容事实仍以
`docs/` 中对应权威文档为准；需要确认设计或工程约束时，按 docs/README.md 查阅对应主题。

## 项目概述

这是一个基于 Godot 和 GDScript 的策略叙事游戏。玩家扮演从 550C、550W 逐步演化为
MOSS 的守护者 AI，在 2044 至 2075 年的危机中权衡文明存续、人类自主权和 MOSS 控制权。

## 快速了解

- 应用入口：`scenes/app_root.tscn`
- 游戏场景：`scenes/main_os.tscn`
- 主控制器：`scripts/systems/main_os.gd`
- 核心数据：`data/` 下的 `.tres` 资源
- 自动化播放测试：`tests/test_runner.tscn` + `tests/test_runner.gd`
- 文档阅读导航：[docs/README.md](docs/README.md)
- 当前路线图：`docs/dev/开发流程.md`
- 解耦专项记录与保留边界：`docs/dev/解耦专项计划.md`
- 产品设计入口：`docs/design/游戏设计.md`

## 工作原则

1. 一个项目事实只在一个权威文档中维护，其他文档使用链接。
2. 明确区分“当前实现”“已确认设计”“待专项设计”和“近期不做”。
3. 游戏配置优先使用 `.tres` 资源，避免把可配置内容硬编码到脚本。
4. 场景结构优先通过 Godot 编辑器或 Godot MCP 调整；谨慎编辑 `.tscn`，修改后必须验证场景可加载。
5. 注释、调试信息、UI 文本、事件数据和项目文档使用中文。
6. 修改 GDScript 前查阅 `docs/dev/代码规范.md`；修改节点职责、场景通信或
   Resource 生命周期前查阅 `docs/dev/Godot开发约定.md`；修改主循环、信号或
   数据类前查阅 `docs/dev/技术架构.md`。
7. 声称完成前，按 `docs/dev/测试指南.md` 执行与改动范围对应的验证。

## Godot MCP 硬门禁

以下门禁只适用于 Godot 代码、场景、资源或测试工作；纯文档任务不要求连接或启动 MCP。

1. 首先通过 `godot-ai` 列出会话，按当前工作目录的规范化绝对路径匹配并激活现有会话，再读取编辑器状态；不得只凭会话名称猜测。
2. 匹配会话为 `ready` 时，必须复用该会话完成场景检查、重载、运行、日志和截图；不得启动第二个 Godot，也不得对同一目录运行独立无头实例。
3. Godot AI v4 使用插件面板为当前客户端生成的 `godot-ai attach` 连接配置；不再以旧的裸 HTTP 地址配置客户端。
4. 外部修改 `.gd`、`.tres` 或 `.tscn` 后，使用 MCP 重新扫描或导入，并确认编辑器中的脚本符号、场景层级和磁盘内容一致。
5. 运行验证必须记录本次 `run_id`、最终测试标记和错误日志。不能用旧日志、早期片段或“场景能打开”代替测试通过。

详细操作和故障恢复见 `docs/dev/Godot开发约定.md`，测试证据要求见 `docs/dev/测试指南.md`。

## 测试与提交

- 提交前完成本任务要求的验证；跨系统重构或明确发布验收采用全量门禁。
- 完成报告一次性列明实际验证结果与未验证项。

整理文档时，合并重复内容；失效计划不再作为当前执行依据。
