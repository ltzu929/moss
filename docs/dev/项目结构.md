# 项目结构

本文档只维护当前目录结构、关键文件职责和放置约定。当前路线见 `开发流程.md`，运行时关系见 `技术架构.md`。

## 目录总览

```text
res://
├── addons/
│   └── godot_ai/                 # Godot MCP 插件
├── assets/
│   └── ui/                       # UI 与事件图片资源
├── data/
│   ├── sector_*.tres             # 区域或机构板块
│   ├── commands/                 # 基础指令资源
│   ├── events/                   # 主事件与中型事件资源
│   ├── situations/               # 三类随机局势与专属方针
│   └── technology/               # 科技节点资源
├── scripts/
│   ├── resources/                # Resource 数据类与区域卡片逻辑
│   ├── systems/
│   │   ├── command_system.gd     # 指令领域规则与效果
│   │   ├── decision_history.gd    # 不可逆核心决策历史
│   │   ├── development_log.gd    # 开发期 JSONL 诊断日志
│   │   ├── situation_system.gd   # 随机局势领域规则
│   │   └── main_os.gd            # 当前游戏主控制器
│   └── ui/                       # 弹窗、地图、地球视图、主题等 UI 脚本
├── scenes/
│   ├── main_os.tscn              # 主场景
│   ├── event_popup.tscn          # 事件报告
│   ├── decision_archive_panel.tscn # 决策档案展示组件
│   ├── situation_panel.tscn      # 非模态局势追踪与方针选择
│   ├── allocate_popup.tscn       # 算力分配
│   ├── technology_screen.tscn    # 全屏科技控制台
│   ├── technology_node_card.tscn # 静态科技节点卡组件
│   ├── command_button.tscn       # 指令按钮
│   ├── sector_info.tscn          # 区域卡片
│   ├── year_progress.tscn        # 日期进度
│   └── game_over.tscn            # 结局界面
├── tests/
│   ├── test_runner.gd            # 完整自动播放回归
│   ├── command_system_test.gd    # 指令领域规则和按钮状态回归
│   ├── development_log_test.gd   # 开发诊断日志落盘和主场景接入
│   ├── event_state_test.gd       # 事件状态写入与重开清理
│   ├── event_playability_test.gd # 事件资源与防软锁约束
│   ├── decision_history_test.gd  # 2044 核心决策纵向切片
│   ├── resource_isolation_test.gd # Resource 运行态隔离
│   ├── mid_event_resource_test.gd # 中型事件资源加载和状态值
│   ├── project_hygiene_test.gd   # 废弃路径和场景加载检查
│   ├── project_conventions_test.gd # 项目代码与 Godot 约定检查
│   ├── technology_system_test.gd # 科技节点图和协议点规则
│   ├── technology_gameplay_test.gd
│   ├── technology_ending_test.gd
│   ├── technology_ui_test.gd
│   ├── situation_system_test.gd  # 局势领域规则与三类资源
│   ├── situation_ui_test.gd      # 局势 HUD 与响应式边界
│   └── world_map_view_test.gd
├── docs/
│   ├── dev/                       # 工程、流程、架构和测试文档
│   ├── design/                    # 产品、内容、科技树、UI 和数值规则
│   ├── lore/                      # 来源分级、时间线素材和原著文本
│   └── worldbuilding/             # 结构化世界观资料、系统映射、缺口和冲突记录
├── project.godot
├── AGENTS.md                     # AI 的 MCP 会话硬门禁
├── .mcp.json                    # 共享 godot-ai HTTP 入口
└── CLAUDE.md
```

## 关键职责

### 数据

- `scripts/resources/game_event.gd` 与 `event_option.gd`：事件和选项数据结构。
- `scripts/resources/sector_data.gd`：区域状态数据。
- `scripts/resources/command_data.gd`：指令消耗、效果与冷却。
- `scripts/resources/tech_node_data.gd`：科技节点、路线、阶段和标签数据。
- `scripts/resources/situation_data.gd` 与 `situation_approach_data.gd`：随机局势模板和持续方针数据。
- `data/`：上述类型的实际资源配置。

### 运行时

- `scripts/systems/main_os.gd`：加载资源、推进年月、处理事件流程、协调指令、同步 UI、判定结局与重新开始。
- `scripts/systems/command_system.gd`：指令查找、科技解锁、参数覆盖、可用性、冷却、执行结算和指令数值效果。
- `scripts/systems/decision_history.gd`：不可逆核心标签、可读档案记录和只写一次约束。
- `scripts/systems/development_log.gd`：开发期 JSONL 诊断日志，写入 `user://moss_development_diagnostics.jsonl`，用于崩溃后定位最后运行状态。
- `scripts/systems/situation_system.gd`：局势随机生成、月度推进、严重度阶段、方针、冷却和结算。
- `scripts/systems/technology_system.gd`：协议点、节点激活、阶段和科技标签。
- `scripts/ui/technology_screen.gd`：索引静态科技场景、同步系统状态及处理不可逆确认交互。
- `scripts/ui/technology_node_card.gd`：科技资源预览、节点状态样式和点击信号。
- `scripts/ui/world_map_view.gd`：可点击世界地图和区域状态绘制。
- `scripts/ui/region_orbital_view.gd`：右侧 3D 地球聚焦视图。
- `scripts/ui/event_popup.gd`：事件报告、选项和延迟影响提示。
- `scripts/ui/decision_archive_panel.gd`：核心决策记录的可复用展示组件。
- `scripts/ui/situation_panel.gd`：最多两项局势的非模态追踪、详情和方针交互。
- `scripts/ui/moss_ui_theme.gd`：当前 HUD 的共享样式。

### 测试

- `tests/test_runner.tscn`：自动化测试场景入口。
- `tests/test_runner.gd`：实例化主场景、加速时间、自动响应弹窗并执行断言。
- `tests/command_system_test.tscn`：检查指令领域规则、效果结算和按钮展示边界。
- `tests/development_log_test.tscn`：检查开发诊断日志 JSONL 写入和主场景启动快照。
- `tests/time_system_test.tscn`：检查月份推进、年度结算、非一月事件和重开日期。
- `tests/event_state_test.tscn`：检查事件选项写入轻量事件状态，以及重开清理。
- `tests/event_playability_test.tscn`：检查全部事件的零能源保底、触发键和运行时防软锁。
- `tests/decision_history_test.tscn`：检查 2044 核心决策写入、2058 回读和结局解释。
- `tests/resource_isolation_test.tscn`：检查区域与指令运行态不会污染其他实例或 `.tres` 模板。
- `tests/mid_event_resource_test.tscn`：检查 17 个中型事件资源的年月、选项和状态值。
- `tests/project_hygiene_test.tscn`：检查废弃路径不会回归，并加载所有保留场景。
- `tests/project_conventions_test.tscn`：检查低误伤的代码规范和 Godot 工程约定。
- `tests/world_map_view_test.tscn`：检查世界地图遮罩、点击和主场景选区同步。
- `tests/situation_system_test.tscn`：检查三类资源、并发、方针、断供、指令干预和固定种子。
- `tests/situation_ui_test.tscn`：检查顶部入口、非模态详情、方针回写、时间控制与双分辨率边界。

## 放置约定

- 可配置游戏数据放在 `data/`，使用带类型的 `.tres` Resource。
- 通用数据结构放在 `scripts/resources/`。
- 当前跨系统流程放在 `scripts/systems/`；是否继续拆分由专项架构评估决定。
- UI 行为和视觉组件放在 `scripts/ui/`；UI 按钮只展示业务系统传入的可用性结果。
- 场景放在 `scenes/`，素材放在 `assets/`。
- 科技树的 `.tres` 保存节点数据，场景实例保存固定资源引用和布局；增删节点必须同步修改两者。
- 自动化入口和测试辅助代码放在 `tests/`。
- 产品与规则文档放在 `docs/design/`，工程文档放在 `docs/dev/`，史料和来源规则放在 `docs/lore/`，结构化世界观资料放在 `docs/worldbuilding/`。
- 不保留阶段性设计包或重复索引；其中仍有持续价值的结论应合并进 `docs/design/`、`docs/dev/`、`docs/lore/` 或 `docs/worldbuilding/` 的权威入口。

## 场景编辑

优先通过 Godot 编辑器或 Godot MCP 修改节点结构和属性。手动编辑 `.tscn` 时必须保持资源引用、节点路径和唯一节点名称有效，并在修改后运行场景加载或自动化测试。
