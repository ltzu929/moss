# 开发流程

本文档维护当前开发顺序和完成标准。产品规则见 `../design/游戏设计.md`，代码事实见 `技术架构.md`。

## 当前主要缺口

| 缺口 | 当前问题 |
|------|----------|
| 运行时授权表达 | 常规授权、危机授权和争议授权已有文档口径，但尚未成为指令数据与稳定日志字段 |
| 时间控制 | 月 tick、手动暂停/继续和局势自动暂停已落地；调速仍未实现 |
| 存档 | 核心标签、轻量状态、局势历史和随机种子均可形成运行时快照，但尚无正式存档读写流程 |
| 社会反馈 | 主事件、分支和结局已有历史回声，普通人新闻、通信与长期生活反馈仍不够密集 |

## 已完成的首版因果闭环

1. 2044、2053、2058、2065、2070 五个核心选择均形成不可逆标签，并被后续主事件或结局读取。
2. 2053 年牺牲外围地下城与 2065 年隐藏核心链路各解锁一个总量受控的条件短分支；未触发时固定时间线仍完整。
3. 托管、混合、人类自主三条代表路线使用真实事件按钮、指令、科技、局势和结局流程完成自动通关。
4. 组合专项测试验证核心标签和后续轻量状态叠加，避免后写状态覆盖早期因果。

## 后续工作顺序

1. 首个可玩版本进入缺陷修复、节奏与人工体验验收，不再以扩充事件数量代替收尾。
2. 调速和存档分别作为独立设计与实现任务；暂停/继续已作为局势系统的必要反馈先行落地。
3. 逐步把三类授权写入指令日志和后续回声，并补普通人生活反馈，不新增复杂政治系统。
4. 按 `解耦专项计划.md` 执行 `main_os.gd` 解耦：先稳定内容身份与测试边界，再处理事件、结局、区域和日志，局势先类型化后拆算法，领域稳定后拆主场景，最后收口主控制器与基础设施。

现有 21 个科技节点暂不因本次事件因果闭环而扩充；这是一条明确边界，不是遗漏。

## 2026-07 工程审计结论

- 已统一共享 MCP 配置到 `godot-ai` HTTP 服务，并在 `AGENTS.md`、`CLAUDE.md` 和 Godot 约定中加入会话硬门禁。
- 已修复事件全选项能源不足时永久等待的软锁；所有事件资源新增零能源保底验收，运行时保留紧急降级。
- 完整通关测试改为点击真实启用按钮，不再绕过 UI 禁用状态；独立测试入口增加导入和逐场景超时。
- 已修复科技互斥组校验中的不可达分支，并加入跨路线错误回归。
- 已隔离 `SectorData` 和 `CommandData` 运行态，避免多个场景实例通过 Godot 资源缓存污染 `.tres` 模板。
- 已把 2044 核心选择与 2055 工程状态改为可叠加修正，并用 3×3 组合回归防止后写状态覆盖早期因果。
- 已加入玩家可见的决策档案、模态暂停和 2044→2058→2065→结局回读闭环。
- 已补齐 2065、2070 核心标签及其后续方案、正文和结局回读，并验证 2065 与 2068 状态的全部 9 种组合。
- 已增加两个单条件短分支及六种分支状态回读；触发层保持受控，不扩展为通用事件树。
- 已将完整通关扩展为托管、混合、人类自主三条代表路线；PR2 阶段统一入口注册 25 个测试场景。
- 随机局势已扩展为九类来源受控内容，包含持续方针、一次性阶段节点、轻量处置历史、一个由科技或事件历史解锁的条件型机会、地图警示和暂停反馈。
- `main_os.gd` 解耦已进入专项执行；PR 1b 已稳定事件、选项和区域的内容身份，PR 1c 已将事件触发、选项查找、区域跨系统传递统一切换到 `event_id`、`option_id` 和 `region_id`，标题、按钮文字和区域名称仅保留在显示与日志边界；PR2 已完成完整月度公开协程、局势暂停只读查询、三条路线测试拆分和私有访问基线收口。

## 2026-08 PR3 执行结论

- 已新增 `EventStateStore`，接管轻量事件状态的写入、查询、深拷贝导出和重开清理；`MainOS` 保留兼容包装，外部快照不能反向污染服务状态。
- 已新增 `EventNarrativeSystem`，迁移主事件历史回声、五类主事件上下文和代表性选项显示后缀；数值调整、事件副本装配和 UI/结算编排仍留在 `MainOS`。
- 已消费事件叙事与结局历史两份生产基线，补充原始资源不变断言；统一入口当前注册 27 个场景。

## 2026-08 PR4 执行结论

- 已新增 `EventResolutionSystem`，接管四个主事件的历史数值调整、`human_event_mitigation` 科技减免，以及能源/板块限幅；`MainOS` 只保留选中板块、能源、UI 和日志写回适配。
- `calculate_option_projections()` 同时返回预览投影与结算投影；事件弹窗消费运行时副本中的选项，结算直接消费玩家选中的同一 `EventOption`，不重新读取原始 `.tres`。
- `event_popup.gd` 的 `popup_event(event, current_energy)` 与 `option_selected(index)` 接口未修改；新增 `event_resolution_test.tscn` 覆盖三组九种组合、2070→2075 三条真实方案、科技减免、边界限幅和资源模板隔离。
- 实现与原计划一致；PR4 验证完成后，统一入口注册数更新为 28 个场景。

## 2026-08 PR5 执行结论

- 已新增 `EndingSystem`，接管四类结局判定、控制权归零失败阈值、结局基础文本、历史回顾和科技摘要；服务只消费显式平均状态、科技能力、核心决策和轻量事件状态快照。
- `MainOS` 保留 `check_game_end()`、`trigger_game_over()`、`trigger_ending()`、`show_end_screen()` 及兼容包装，只负责快照装配、Timer/信号/日志/UI 编排；结局正文继续使用普通 `Label` 可直接显示的文本，不含 BBCode。
- `technology_ending_test.gd` 的 3 处跨脚本私有摘要访问已改为 `get_technology_summary()`，私有访问基线同步删除；`ending_history_baseline.json`、`decision_history_test` 和 `branch_event_test` 的历史回读路径保持逐字不变。
- 实现与原计划一致；统一入口仍注册 28 个场景，下一步为事件领域 PR6 收口与协调器清理。

## 2026-08 PR6 执行结论

- 将 `event_state_test` 收口为状态服务隔离、真实状态写入和重开清理；叙事基线与数值服务矩阵不再和状态测试重复。
- 新增 `event_writeback_test.tscn`，通过真实 `EventPopup` 和 `MainOS.process_month_tick()` 覆盖 2070→2075 三条方案的历史后缀、运行时副本、板块/能源/行动日志写回、稳定 ID、0/100 边界和能源上限。
- 删除 `MainOS.apply_event_option_adjustments()` 的重复兼容层，`build_display_event()` 直接把同一运行时副本交给 `EventNarrativeSystem` 与 `EventResolutionSystem`；场景写回、Timer/await、状态和决策兼容接口保持不变。
- 实现与原计划一致；统一入口当前注册 29 个场景，下一步转入局势领域 PR7。
- PR6 当次 MCP 会话中除三条完整路线外的 26 个独立场景均以最终标记和失败断言 0 结束；三条路线当时因 `Timer.time_left` 停滞未计为通过。2026-08-09 已在同一项目的唯一 `ready` 会话中补跑当前注册表 29 个场景，混合、托管、人类自主路线分别以 `r6843949-98`、`r6656954-91`、`r6689384-92` 产生最终标记并以失败断言 0 结束，运行态补验已闭环。

## 2026-08 PR7 执行结论

- 新增 `ActionLogView` 场景/脚本，接管行动日志显示标签、打字机队列、同步容量淘汰、滚动、光标和重开代次；`MainOS.record_action()` 与 `get_action_log()` 的领域协议、24 条领域容量和玩家可见文本保持不变。
- 主场景保留 `LogPlaceholder` 布局路径，仅把原日志节点迁入 `scenes/action_log_view.tscn`；`MainOS` 通过 `append_entry()`/`clear()` 调用组件，不访问 `%LogEntryContainer`、`%LogCursor` 或显示队列内部。
- `action_log_ui_test.tscn` 改为公开接口 + 真实节点结果验证，移除 19 次测试私有访问；机器基线由审计工具刷新为 32 个测试文件、27 个条目、52 次访问。
- 当前 MCP 会话专项运行 `r10055492-125` 通过 28 条断言、最终标记 `[MOSS-ACTION-LOG-UI]`、失败断言 0；同一 `ready` 会话随后按注册表顺序运行全部 29 个场景，run_id 后缀为 104–132，三条路线分别为 `r10109787-130`、`r10138924-131`、`r10166826-132`，均以最终标记、失败断言 0 和停止态结束；内容身份审计、私有访问 `--fail-on-new`、47 个 Python 运行器/审计单测和 `compileall` 均通过。
- 实现与原计划一致；统一入口仍注册 29 个测试场景，下一步进入局势运行态类型化 PR8。

## 2026-08 PR8 执行结论

- 新增 `SituationInstanceState`，将 `_active` 从 `Array[Dictionary]` 改为类型化运行态；`SituationSystem` 保留原有 UI/主循环 Dictionary 协议，新增稳定 ID 资格查询，以及不含 Resource 引用的运行态导出/校验恢复接口。
- 运行态恢复在提交前完成版本、种子/RNG、模板与实例/区域/方针/节点 ID、严重度阶段、年月、冷却和历史结构校验；非法输入失败时保持原状态不变。公式、固定种子序列、反向结算顺序、历史回声、重开清理和正式存档边界未改变。
- `situation_system_test` 以 `r17432782-155` 通过合法/非法快照、失败原子性和公开资格接口；`situation_ui_test` 以 `r17857862-169`、`action_log_ui_test` 以 `r18012258-173`、`technology_gameplay_test` 以 `r18342642-178`、`technology_ending_test` 以 `r18392497-180` 均以失败断言 0 结束。其余注册场景也逐项以当前 `run_id`、最终标记和停止态通过。
- 三条完整路线均通过：mixed `r18125798-175`、managed `r18200756-176`、human autonomy `r18270037-177`，均为最终标记和失败断言 0。
- 静态门禁通过：53 个 Python 运行器/审计单测、`python -m compileall -q tools`、内容身份审计、私有访问 `--fail-on-new`（32 个测试文件、24 个条目、46 次访问）和 `git diff --check`。重载主场景后，编辑器层级仍为 23 个节点；`SituationSystem` 符号为 48 个函数，新类型脚本成功识别；编辑器游标 7 之后没有新增错误。
- 实现与原计划一致；统一入口仍注册 29 个测试场景，下一步进入局势算法服务拆分 PR9。

## 2026-08 PR9 执行结论

- 新增 `SituationSpawnPlanner`、`SituationFundingPlanner`、`SituationMonthResolver` 和
  `SituationSnapshotBuilder` 四个无场景树依赖的 `RefCounted` 服务；`SituationSystem` 保留
  活跃集合、RNG、冷却/历史写入和资源/集合事务编排，`MainOS` 月度调用与 UI Dictionary 协议不变。
- 新增 `situation_algorithm_test.tscn`，以真实局势资源覆盖候选资格、固定种子候选、竞争资源计划、
  零资源断供、阶段跨越、机会节点和快照字段；本次变更不修改局势资源、数值、通知文本、月度顺序、
  事件/结局语义或正式存档范围。
- 最终统一入口 30 个场景按注册表逐项通过，最终全量 run_id 后缀为 `211–240`；算法服务
  `r30116730-223`、局势 UI `r30406912-232`、混合/托管/人类自主三条路线分别为
  `r30508315-238`、`r30547903-239`、`r30600641-240`，均以最终标记、失败断言 0 和停止态结束。
  修复 funding plan 传递问题后，局势 UI 复核 `r29622817-207`、算法/系统定向复核
  `r30856633-241`、`r30892129-242` 也均通过。
- 静态门禁通过：53 个 Python 运行器/审计单测、`python -m compileall -q tools`、内容身份审计、
  私有访问 `--fail-on-new`（24 个条目、基线次数未增长）和 `git diff --check`。外部修改后重新扫描
  得到 88 个全局类；`SituationSystem` 为 39 个函数，编辑器错误游标 7 之后没有新增记录。
- 实现与原计划一致；统一入口当前注册 30 个测试场景，下一步为 PR10 HUD 与战略工作区组件化。

## 实现验收原则

- 新系统必须服务长期选择，而不是只增加按钮或数值。
- 已实现状态必须有代码、场景和专项测试证据。
- UI 调整必须同时验证 1920×1080 和较小视口的响应式边界。
- `@tool` 预览必须在编辑器中可见，运行时接口不得因此改变。
- 外部文件编辑后必须重新导入，并通过当前 Godot 会话运行测试。
- 不启动第二个 Godot，不在编辑器已打开时运行独立无头测试。

## 近期明确不做

- 回合制时间重构。
- 新增第五类结局。
- 将本轮九个新增科技接入事件内容。
- 未经专项计划批准的额外 `main_os.gd` 拆分。
